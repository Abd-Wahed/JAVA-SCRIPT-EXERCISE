JavaScript

১. map()
📌 কাজ: অ্যারের প্রতিটি আইটেমের উপর কাজ করে এবং নতুন অ্যারে রিটার্ন করে।
const nums = [1, 2, 3];
const doubled = nums.map(n => n * 2);
console.log(doubled); // [2, 4, 6]
🧠ব্যবহার করো যখন:
    -সবগুলো আইটেমে কিছু পরিবর্তন করতে চাও (e.g., গুণ, যোগ, স্ট্রিং পরিবর্তন)
    -নতুন অ্যারে দরকার হয়

 ২. filter()
📌 কাজ: নির্দিষ্ট শর্ত অনুযায়ী অ্যারের কিছু আইটেম ফিল্টার করে নতুন অ্যারে দেয়।
const nums = [1, 2, 3, 4, 5];
const even = nums.filter(n => n % 2 === 0);
console.log(even); // [2, 4]
🧠ব্যবহার করো যখন:
   - কিছু আইটেম বাদ দিয়ে বাকি অংশ রাখতে চাও
   - শর্ত অনুযায়ী একটি subset দরকার হয়

 ৩. reduce()
📌 কাজ: সব আইটেম নিয়ে একটা একক মান (value) তৈরি করে।
const nums = [1, 2, 3, 4];
const sum = nums.reduce((acc, current) => acc + current, 0);
console.log(sum); // 10
🧠ব্যবহার করো যখন:
   - সব ডেটা মিশিয়ে একটা মান দরকার (যেমন: যোগফল, গড়, গুনফল)
   - object হিসেবেও তৈরি করতে পারো (e.g., group by, count)

forEach()
📌 কাজ: প্রতিটি আইটেমে শুধু কাজ করে, কিন্তু কিছু return করে না।
const nums = [1, 2, 3];
nums.forEach(n => console.log(n * 2)); // Prints 2, 4, 6
🧠 ব্যবহার করো যখন:
    - শুধু console.log বা কিছু কাজ করতে চাও
    - নতুন অ্যারে দরকার নেই
    - return দিলেও সেটা কাজ করে না